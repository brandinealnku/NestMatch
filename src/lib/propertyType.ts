import type {PropertyType} from '../types/models'; export function normalizePropertyType(value:unknown):PropertyType {const v=String(value??'').toLowerCase();if(v.includes('single'))return 'Single-family';if(v.includes('condo'))return 'Condo';if(v.includes('town'))return 'Townhouse';if(v.includes('multi')||v.includes('duplex'))return 'Multi-family';if(v.includes('manufact')||v.includes('mobile'))return 'Manufactured';return 'Other'}
