import {describe,expect,it,beforeEach} from 'vitest';import {currency,date} from '../lib/format';import {evaluateListing,filterListings} from '../lib/filter';import {defaultCriteria} from '../lib/defaults';import {demoListings} from '../data/demo-listings';import {calculateMatchScore} from '../scoring/calculateMatchScore';import {normalizePropertyType} from '../lib/propertyType';import {DemoListingProvider,normalizeListing} from '../providers/providers';import {storage,undoDecision} from '../storage/repository';import {sortListings} from '../lib/sort';
describe('formatting',()=>{it('formats US currency',()=>expect(currency(425000)).toBe('$425,000'));it('handles bad dates',()=>expect(date('bad')).toBe('Not listed'))});
describe('filtering',()=>{const base=demoListings[0];it('filters max price',()=>expect(evaluateListing({...base,price:600000},defaultCriteria).eligible).toBe(false));it('filters bedrooms and bathrooms',()=>expect(evaluateListing({...base,bedrooms:1,bathrooms:1},defaultCriteria).failures.length).toBeGreaterThan(1));it('handles missing values when permitted',()=>expect(evaluateListing({...base,bedrooms:undefined},defaultCriteria).eligible).toBe(true));it('rejects missing when disabled',()=>expect(evaluateListing({...base,bedrooms:undefined},{...defaultCriteria,includeMissing:false}).eligible).toBe(false));it('returns only eligible',()=>expect(filterListings([{...base,price:999999},base],defaultCriteria)).toHaveLength(1))});
describe('normalization',()=>{it('normalizes provider home types',()=>expect(normalizePropertyType('Town Home')).toBe('Townhouse'));it('normalizes provider responses',()=>expect(normalizeListing({id:'x',price:2,addressLine1:'A',propertyType:'Condo'})?.source).toBe('rentcast'));it('rejects malformed provider response',()=>expect(normalizeListing({id:'x'})).toBeNull())});
describe('scoring',()=>{it('keeps boundaries',()=>{const s=calculateMatchScore(demoListings[0],defaultCriteria);expect(s.total).toBeGreaterThanOrEqual(0);expect(s.total).toBeLessThanOrEqual(100)});it('gives ideal budget full budget points',()=>expect(calculateMatchScore({...demoListings[0],price:400000},defaultCriteria).breakdown[0].earned).toBe(30));it('scores square footage smoothly',()=>{const s=calculateMatchScore({...demoListings[0],squareFeet:1500},defaultCriteria);expect(s.breakdown.find(x=>x.name==='Square footage')?.earned).toBeLessThan(15)});it('does not score missing HOA',()=>expect(calculateMatchScore({...demoListings[0],hoaFeeMonthly:undefined},defaultCriteria).unavailableCriteria.some(x=>x.criterion==='HOA')).toBe(true));it('normalizes when fields missing',()=>expect(calculateMatchScore({...demoListings[0],hoaFeeMonthly:undefined,yearBuilt:undefined},defaultCriteria).total).toBeGreaterThan(0));it('generates explanations and labels',()=>{const s=calculateMatchScore(demoListings[1],defaultCriteria);expect(s.positiveReasons.length).toBeGreaterThan(0);expect(s.label).toMatch(/match/)})});
describe('providers, storage, decisions and sorting',()=>{beforeEach(()=>localStorage.clear());it('demo provider returns 24+',async()=>expect((await new DemoListingProvider().searchListings(defaultCriteria)).listings.length).toBeGreaterThanOrEqual(24));it('recovers corrupted storage',()=>{localStorage.setItem('nestmatch:v1:criteria','bad');expect(storage.getCriteria()).toBeNull()});it('undoes latest decision',()=>expect(undoDecision([{listingId:'a',kind:'love',savedAt:''},{listingId:'b',kind:'pass',savedAt:''}])).toHaveLength(1));it('sorts by low price',()=>expect(sortListings([demoListings[2],demoListings[0]],'price-low',new Map(),[])[0].price).toBeLessThanOrEqual(sortListings([demoListings[2],demoListings[0]],'price-low',new Map(),[])[1].price))});
