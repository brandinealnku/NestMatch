import {Link,useNavigate} from 'react-router-dom';import {HouseArt} from '../components/HouseArt';import {storage} from '../storage/repository';export function LandingPage(){const nav=useNavigate();return <div className="landing"><section className="hero"><div><p className="eyebrow">A clearer path to your next address</p><h1>Swipe less.<br/><em>Find the home that fits.</em></h1><p className="lede">Turn what matters to you into a transparent property match. Filter the noise, understand every score, and shape a shortlist with confidence.</p><div className="button-row"><Link className="button primary" to="/preferences">Start matching <span>→</span></Link><button className="button secondary" onClick={()=>{storage.saveSettings({mode:'demo'});nav('/discover')}}>Explore demo homes</button></div><p className="fine">No account. No tracking. Your choices stay in this browser.</p></div><div className="hero-card"><div className="float-score"><b>92%</b><span>Excellent match</span></div><HouseArt variant={1}/><div className="preview-info"><p>$429,000</p><strong>118 Willow Lantern Way</strong><span>3 beds · 2.5 baths · 1,940 sq ft</span></div></div></section><section className="benefits" aria-labelledby="benefits"><div><p className="eyebrow">A better way to browse</p><h2 id="benefits">Make every home earn its place.</h2></div>{[['01','Set your non-negotiables','Start with budget, space, and property type—the practical details that shape your search.'],['02','Understand every match','See exactly where a home fits, where it compromises, and how missing data is handled.'],['03','Build a smarter shortlist','Save the promising ones, compare your favorites, and revise your criteria anytime.']].map(x=><article key={x[0]}><span>{x[0]}</span><h3>{x[1]}</h3><p>{x[2]}</p></article>)}</section></div>}
