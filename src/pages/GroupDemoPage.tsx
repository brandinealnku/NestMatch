import { Link } from "react-router-dom";
import { useCollaboration } from "../collaboration/CollaborationContext";
export function GroupDemoPage() { const { swipes, matches, resetDemo } = useCollaboration(); return <section className="page narrow"><p className="eyebrow">Couple Match Demo</p><h1>Our Home Search</h1><div className="notice collaboration-notice"><strong>Collaborative Demo</strong><span>Alex is a simulated partner. No real account or invitation is connected.</span></div><p className="lede">You and Alex share the same fictional home deck, but review it independently. Alex’s choices stay hidden unless you both Love the same home.</p><div className="privacy-card"><span aria-hidden="true">🔒</span><div><h2>Your decisions are private</h2><p>Your choices stay private unless you both Love the same home. Alex’s unmatched decisions and progress are never shown.</p></div></div><div className="group-actions"><Link className="button primary" to="/group/demo/discover">{swipes.length ? "Continue shared deck" : "Review homes together"}</Link><Link className="button secondary" to="/group/demo/matches">Matches ({matches.length})</Link><button className="button secondary" onClick={() => void resetDemo()}>Reset Demo</button></div></section>; }
