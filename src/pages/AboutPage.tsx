import {useState} from 'react';import {useApp} from '../app/AppContext';export function AboutPage(){const app=useApp();const [confirm,setConfirm]=useState(false);return <section className="page prose"><p className="eyebrow">Trust through transparency</p><h1>About NestMatch</h1><p className="lede small">NestMatch helps you organize a home search around explicit property and financial criteria—not personal demographics.</p><div className="prose-grid"><article><h2>How matching works</h2><p>Every eligible home receives a deterministic score. Budget contributes 30 points; bedrooms 15; bathrooms 10; square footage 15; property type 10; HOA 8; year built 7; and days on market 5. Missing optional data is removed from the denominator, never treated as zero.</p></article><article><h2>Fair-housing by design</h2><p>We do not collect, infer, filter, or score race, religion, national origin, sex, familial status, disability, or other protected characteristics. Neighborhood demographics are not matching inputs.</p></article><article><h2>Your privacy</h2><p>No accounts, cookies, analytics, or geolocation. Criteria and decisions live in local storage in this browser. Provider credentials belong only in the server-side proxy.</p></article><article><h2>Data responsibility</h2><p>Demo homes are fictional. Live availability depends on authorized provider coverage and configuration. Verify every property detail independently.</p></article></div><div className="disclaimer"><strong>Important</strong><p>NestMatch is not a brokerage, lender, appraisal service, or inspection service. It is a discovery and organization tool.</p></div><section><h2>Reset your data</h2><p>Delete all criteria, listings, settings, and decisions stored by NestMatch in this browser.</p>{!confirm?<button className="button danger" onClick={()=>setConfirm(true)}>Reset NestMatch</button>:<div className="confirm" role="alertdialog" aria-label="Confirm reset"><p><strong>Delete all local NestMatch data?</strong> This cannot be undone.</p><button className="button danger" onClick={()=>{app.reset();setConfirm(false)}}>Yes, delete everything</button><button className="button secondary" onClick={()=>setConfirm(false)}>Cancel</button></div>}</section></section>}
