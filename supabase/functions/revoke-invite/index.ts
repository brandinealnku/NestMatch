import "jsr:@supabase/functions-js/edge-runtime.d.ts"; import {context,cors,json,objectBody} from "../_shared/http.ts";
Deno.serve(async req=>{const origin=req.headers.get("origin");if(req.method==="OPTIONS")return new Response(null,{status:204,headers:cors(origin)});if(req.method!=="POST")return json({error:"Method not allowed"},405,origin);try{const {user,admin}=await context(req),body=await objectBody(req),groupId=typeof body.groupId==="string"?body.groupId:"";const {data:m}=await admin.from("group_members").select("role,status").eq("group_id",groupId).eq("user_id",user.id).maybeSingle();if(m?.role!=="owner"||m.status!=="active")return json({error:"Not permitted"},403,origin);const {error}=await admin.from("invitations").update({revoked_at:new Date().toISOString()}).eq("group_id",groupId).is("accepted_at",null).is("revoked_at",null);if(error)throw new Error("store");return json({revoked:true},200,origin)}catch(e){const kind=e instanceof Error?e.message:"";return json({error:kind==="auth"?"Authentication required":"Unable to revoke invitation"},kind==="auth"?401:kind==="origin"?403:400,origin)}});
